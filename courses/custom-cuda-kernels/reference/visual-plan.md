# Visual plan

The table defines this course's overview diagrams. The
[visual manifest](visual-manifest.json) links additional detailed diagrams to
their conceptual lessons. Each figure appears within the specified lesson section or after the specified lab section in its declared lesson or lab home,
with accessible labels, captions, and fit-to-width sizing.

The host-API and device-kernel diagram is inside **How it works** in Lesson 1.
CPU and CUDA paths are distinct; the CUDA path separates interface validation,
submission and device execution. Allocation, transfers, completion and the
consumer-facing package contract remain application responsibilities.

| Title | First stage | Second stage | Third stage | Explanation | Lesson | After | Layout | Home |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Library-first decision | Framework/compiler | cuBLAS, CUB, CUTLASS | Narrow custom kernel | Read downward only while the required capability remains unavailable. Use a maintained path when it meets the contract; own a narrow CUDA implementation only for a meaningful residual gap. The arrows show evaluation order, not mandatory execution stages. | 1 | How it works | flow | lesson |
| CUDA execution | Grid and blocks | Warps on SMs | Memory and pipelines | Read downward from logical work to hardware execution. Blocks contain warps, blocks are assigned to SMs, and ready warp instructions use memory and arithmetic pipelines. A software thread is not a physical core. | 3 | How it works | flow | lesson |
| Fusion traffic | Separate launches | Register-resident fusion | One output write | Compare separate and fused designs and their final-store requirement. Fusion can remove intermediate reads/writes and launches; cache hits determine how much physical HBM traffic was present to save. Required external outputs must still be written. | 5 | How it works | comparison | lesson |
| Coalesced transpose | Row-wise global load | Padded shared tile | Row-wise global store | Follow the data downward: load contiguous global rows, synchronize the shared tile, then read its transposed indices for contiguous output stores. A padding column changes the FP32 tile's bank mapping; partial tiles still require every barrier participant. | 6 | How it works | flow | lesson |
| Hierarchical reduction | Thread-local contributions | Warp and block partials | Combine block results | Read downward through a custom reduction. Local aggregation reduces global contention, with a block barrier before cross-warp reads. CUB is a separate maintained implementation to compare against, not a final stage inside this custom kernel. | 7 | How it works | flow | lesson |
| Stencil tile | Interior and halo loads | Shared reuse | Boundary-safe outputs | Arrows show load, synchronize and consume. The interior plus halo supplies overlapping neighborhoods; invalid global positions use the declared boundary value. Threads without an output may still be required at the barrier. | 8 | How it works | flow | lesson |
| Imbalance diagnosis | Active lane masks | Block-duration skew | Final grid wave | Compare three causes of idle capacity: lane masks, unequal block durations and too few remaining blocks. A remedy at one level need not help the others, and regrouping or queues add costs of their own. | 10 | How it works | comparison | lesson |
| Resource tradeoff | Registers and shared memory | Blocks that fit on an SM | Ready work and elapsed time | The arrows connect resource demand to residency, then to possible execution. More resident warps help only when useful work is ready. Register caps may cause spills and extra memory waits instead of a speedup. | 9 | How it works | flow | lesson |
| Double-buffer pipeline | Load next tile | Compute current tile | Swap stages | Time runs downward. Tile i computes while an independent copy prepares tile i+1. The dependency arrow permits its computation only after that copy completes. Prime before this interval, release stages after use and drain at the end; no measured overlap is implied. | 11 | How it works | pipeline | lesson |
| Library epilogue | Library GEMM machinery | Bias plus activation | Final store | Read downward through GEMM and output processing. A compatible fused epilogue can avoid a separate output read/write. Lab 09 compares cuBLAS plus an epilogue with CUTLASS FP32 SIMT and expanded bias; it does not isolate Hopper Tensor Core or epilogue-only gains. | 12 | How it works | flow | lesson |
| H100 advanced path | TMA tile movement | Cluster scheduling | Cluster shared-memory access | Compare distinct Hopper capabilities. TMA moves described tiles; clusters establish cooperating block groups; distributed shared memory needs the cluster lifetime and synchronization rules. Lab 10 tests cluster launch only. | 15 | How it works | comparison | lesson |
| Acceptance flow | Reference and tests | Sanitizer and profiler | End-to-end decision | Read downward through evidence before integration. Reference tests check results, sanitizers check exercised memory/synchronization behavior, and profiles explain the candidate. Separate unprofiled end-to-end trials establish useful application value. | 14 | How it works | flow | lesson |
| CUDA build and load | Source and target selection | Compile and link artifact | Driver loads device code | Read downward: compile host and device portions, assemble or retain PTX as selected, then link the program. At execution, the driver loads compatible device code or compiles supported PTX. Build success does not prove that a kernel completed correctly. | 2 | How it works | flow | lesson |
| Kernel evidence sequence | Reference output checks | Memory and race checks | Profile, then time alone | Arrows show evidence order. Compare results with a trusted reference, check exercised memory and synchronization behavior, then use profiling to explain performance. Separate unprofiled timing determines elapsed-time claims; each step can reveal a reason to stop and repair the candidate. | 4 | How it works | flow | lesson |
| Residual normalization | Add each residual pair | Reduce squared row values | Normalize and apply weights | Read downward within one row. Form r from the input and residual, compute the mean of r squared, add epsilon and take the reciprocal square root, then multiply each r by that common scale and its learned weight. The reduction must finish before outputs use its scale. | 13 | How it works | flow | lesson |
| Tile programming model | Choose tile coordinates | Load and compute tile | Store valid output lanes | Arrows show logical data flow. Coordinates select a region, tile operations describe its computation, and guarded stores keep boundary positions valid. The compiler maps these operations to GPU execution resources; a logical tile is not a new physical hardware unit. | 16 | How it works | flow | lesson |
