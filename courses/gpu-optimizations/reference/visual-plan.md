# Visual plan

The table defines this course's overview diagrams. The
[visual manifest](visual-manifest.json) links additional detailed diagrams to
their conceptual lessons. Each figure appears within the specified lesson section or after the specified lab section in its declared lesson or lab home,
with accessible labels, captions, and fit-to-width sizing.

The end-to-end performance-system diagram is inside **How it works** in Lesson 1,
connecting the introductory CPU preparation, dispatch, transfer, GPU work and
completion explanation to the limiter model developed later.

| Title | First stage | Second stage | Third stage | Explanation | Lesson | After | Layout | Home |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Causal optimization loop | Freeze baseline | Change one factor | Remeasure and decide | Follow the arrows downward, then return to the baseline for another controlled trial. The workload and correctness contract stay fixed while one factor changes; remeasurement supports a keep, reject or inconclusive decision. | 1 | How it works | cycle | lesson |
| Profiler evidence funnel | Framework attribution | System timeline | Selected kernel counters | Read downward as an investigation order, narrowing only when the next question needs more detail. Framework events identify operations, the timeline exposes dependencies, and kernel counters explain a selected operation. These are views of execution, not stages the workload passes through. | 3 | How it works | flow | lesson |
| Asynchronous timing | CPU submits work | GPU reaches queued work | Required work completes | Read downward in event order. Submission can finish while device work remains; a completed-work timer stops only after its required result is ready. The spacing does not represent duration or prove overlap. | 2 | How it works | timeline | lesson |
| Launch and fusion | Separate small kernels | Fused computation | Complete application result | Compare the separate and fused implementations, then their application result. Fusion can remove launches and intermediate traffic, but the complete result improves only when those savings exceed added costs. The boxes are comparison topics, not a sequence of kernel execution. | 4 | How it works | comparison | lesson |
| Input pipeline | Storage and CPU preparation | Pinned host-to-device copy | GPU consumes the batch | Arrows show one batch's dependencies: prepare it, complete its input copy, then consume it. Different batches may overlap with suitable streams, hardware and ownership; this stage map does not itself depict overlap. | 6 | How it works | flow | lesson |
| Allocator lifetime | Tensor uses storage | Allocator can cache it | Later allocation may reuse it | Read downward through one allocation's lifetime. After the last tensor and asynchronous user release it, eligible storage can remain cached. Reserved bytes include active allocations as well as cached capacity. | 8 | How it works | flow | lesson |
| Shape and precision | Shape, layout and dtype | Library selects a kernel | Time, memory and error | The arrows connect input properties to implementation selection and its consequences. Padding or casting adds work of its own; a faster selected kernel must still preserve the useful result and repay those costs. | 9 | How it works | flow | lesson |
| Collective overlap | Backward work | Ready bucket | Exposed wait | Time runs downward in both columns. A ready bucket begins communication before independent backward work ends. Only the unfinished tail then delays the dependent update. Bar lengths are schematic, not measurements. | 12 | How it works | overlap | lesson |
| Tail diagnosis | Lane divergence | Block or rank skew | Final partial wave | Compare where capacity becomes unused. Divergence affects lanes within a warp, skew affects whole tasks or ranks, and a partial final wave can occur even with equal-duration blocks. Each requires evidence at its own level. | 10 | How it works | comparison | lesson |
| Library-first decision | Framework or compiler | Maintained library | Narrow custom CUDA gap | Read downward only while an important gap remains. Stop at a maintained option that satisfies the operation and performance contract. The final custom path inherits correctness, coverage and maintenance obligations. | 13 | How it works | flow | lesson |
| Execution-plan reuse | Warm up and retain buffers | Capture and instantiate | Refresh values and replay | Read downward through preparation and reuse. Warm-up resolves lazy work, capture records an eligible dependency graph, and instantiation prepares an executable plan. Replays reuse its eligible structure and storage addresses while input values may change; replay does not make dependent operations concurrent. | 5 | How it works | flow | lesson |
| Layout and traffic choices | View: change addressing | Copy: move into new layout | Fusion: avoid intermediates | Compare three mechanisms, not mandatory stages. A view changes strides without moving values; a consumer may require a real layout copy; fusion can keep intermediate values within one kernel. Include the complete producer-to-consumer path before deciding which removes actual traffic. | 7 | How it works | comparison | lesson |
