# Visual plan

The table defines this course's overview diagrams. The
[visual manifest](visual-manifest.json) links additional detailed diagrams to
their conceptual lessons. Each figure appears within the specified lesson section or after the specified lab section in its declared lesson or lab home,
with accessible labels, captions, and fit-to-width sizing.

The autoregressive lifecycle is inside **How it works** in Lesson 1 and remains
linked from Lesson 2. Its first token comes from prefill; subsequent decode
processes the selected token only when generation continues. Token choice,
stopping, buffering and completion are explicit parts of the same workflow.

| Title | First stage | Second stage | Third stage | Explanation | Lesson | After | Layout | Home |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Artifact loading | Identify the complete bundle | Load, check and warm it | Accept the intended requests | Read downward through preparation. The configuration, tokenizer, weights, adapters and quantization must agree. A live process is not sufficient readiness; startup and warm-up remain separate from steady serving. | 1 | How it works | flow | lesson |
| Prefill and decode | Prefill builds prompt KV | First token from prefill logits | Decode subsequent tokens | Read downward in event order. Prefill supplies the first output distribution; only if generation continues does a forward process the sampled token and append its KV. The spacing shows order, not measured time. | 2 | How it works | timeline | lesson |
| KV capacity | Layer and KV-head dimensions | Bytes for stored tokens | Remaining request capacity | The arrows show the calculation: first derive bytes per token, then multiply by stored tokens. Admission also needs room for weights, workspaces, pools and overhead; the ideal KV formula is not total peak memory. | 4 | How it works | flow | lesson |
| ISL and OSL matrix | Short ISL | Long ISL | Fixed concurrency per panel; repeat the matrix at other loads | Rows vary prompt length (ISL), and columns vary generated length (OSL). Each cell is a different workload; repeat the matrix at other concurrency or arrival rates rather than treating load as another cell. | 5 | How it works | matrix | lesson |
| Service metrics | Submission, queue and prefill | First output token arrives | Later output tokens arrive | Read downward through observable events. TTFT ends at the first token; ITL measures later token gaps. Protocol completion may occur after the last content token, and transport chunks do not necessarily identify individual tokens. | 7 | How it works | timeline | lesson |
| Paged KV | Free physical blocks | Request block mappings | Release ownership; recycle | Follow allocation and release around the loop. Completion releases a request's ownership; a block returns to the free pool only when no active user or retention policy needs it. Partial final blocks still waste capacity. | 8 | How it works | cycle | lesson |
| Continuous batching | Admit requests | Mix prefill and decode | Retire and refill | Follow one scheduling round downward, then repeat. Admission needs token budget and KV capacity; finished requests leave, making room for new work. Chunked prefill changes how prompt work shares rounds with decode. | 9 | How it works | cycle | lesson |
| Prefix reuse | Exact token prefix | Cached KV blocks | Remaining prefill | Read downward only for an exact compatible hit. Reuse skips the matched complete prefix and computes the remaining prompt. A missing, expired or incompatible entry needs recomputation; a slow-tier hit also needs a justified restore. | 10 | How it works | flow | lesson |
| Speculative decoding | Draft proposals | Target verification | Accept or recover | The draft proposes a sequence, and the target verifies its conditional choices. Accept only a consecutive prefix; at the first rejection, emit the valid recovery token and discard later proposals. Draft, verify and recovery costs all affect the result. | 13 | How it works | flow | lesson |
| Inference placement | Replicas or partitions | Communication path | Latency and capacity | The arrows connect a placement choice to its communication and service consequences. Replicas handle independent requests; partitions cooperate within a request. More workers can improve fit while adding latency to token generation. | 14 | How it works | flow | lesson |
| Serving stack | Client and metrics | Engine scheduler | H100 kernels and KV | Read downward through a request's execution layers. The client observes the service boundary, the engine schedules work and KV state, and the device executes kernels. Client metrics do not by themselves identify the internal bottleneck. | 6 | How it works | flow | lesson |
| Disaggregation | Prefill workers | KV handoff | Decode workers | Read downward in the direction of the KV handoff: prefill produces state, transfer/reconstruction makes it available, and decode continues the request. Coordination can travel separately in either direction. Added handoff and queueing costs can exceed specialization gains. | 15 | How it works | flow | lesson |
| Decoding policy | Scores become probabilities | Filter and select token | Apply stopping rule | Arrows show policy order. Apply the declared temperature and filtering before selection; greedy selection chooses the highest eligible score, while sampling draws from the eligible distribution. Append the chosen token and evaluate stopping rules before continuing. The policy and random-state convention determine the output contract. | 3 | How it works | flow | lesson |
| Quantized representation | Values and chosen scales | Low-bit codes and metadata | Supported computation path | Read downward from representation to use. A scale and optional offset map values to limited codes. Storage contains both those codes and required metadata. A supported kernel reconstructs values or computes directly with the representation; rounding and clipping must remain within the quality contract. | 12 | How it works | flow | lesson |
| Inference decision evidence | Fixed baseline and workload | One controlled candidate | Keep, reject or inconclusive | Arrows show evaluation order, not guaranteed improvement. Compare a candidate against fixed workload and output semantics, then check quality, latency, throughput, capacity and failure evidence. Keep only a supported improvement; reject a harmful change or remain inconclusive when evidence is insufficient. | 16 | How it works | flow | lesson |
