# Changelog

## [Unreleased]

- Imported the Soperator 3.0.3 K8up backup schedule as a pinned companion
  chart.
- Added an explicit `enabled` gate and fail-fast validation for required bucket
  and Secret reference fields.
