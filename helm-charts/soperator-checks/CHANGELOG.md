# Changelog

## [Unreleased]

- Imported the Soperator 3.0.3 checks controller chart as a pinned companion
  chart.
- Disabled `ServiceMonitor` by default and changed the manager pull policy to
  `IfNotPresent`.
