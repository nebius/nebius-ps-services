# Changelog

## [Unreleased]

- Imported the Soperator 3.0.3 DCGM job-mapping exporter as a pinned optional
  companion chart.
- Disabled `ServiceMonitor` by default and moved the busybox init image into
  values.
