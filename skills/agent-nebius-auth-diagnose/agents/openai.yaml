interface:
  display_name: "Agent Nebius Auth Diagnose"
  short_description: "Diagnose Nebius auth and quota reads"
  default_prompt: "Use $agent-nebius-auth-diagnose to diagnose current-session Nebius auth and tenant quota-read access without mutation, correct retryable command denials, and route persistent repair safely."
policy:
  allow_implicit_invocation: true
