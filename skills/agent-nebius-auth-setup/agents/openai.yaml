interface:
  display_name: "Agent Nebius Auth Setup"
  short_description: "Explicitly set up Nebius agent auth"
  default_prompt: "Use $agent-nebius-auth-setup to explicitly converge the fixed codex-agent-sa account with project admin, tenant viewer, its project-bound credential and profile, or an explicitly requested bounded local-repair lease."
policy:
  allow_implicit_invocation: false
