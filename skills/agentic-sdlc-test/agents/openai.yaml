interface:
  display_name: "Agentic SDLC Test"
  short_description: "Verify deterministic and live SDLC evidence"
  default_prompt: "Use $agentic-sdlc-test to verify Agentic SDLC source-installed parity, deterministic prompt/execution/worktree/hook capabilities, and any private live-results evidence, then write the safe readiness report."
policy:
  allow_implicit_invocation: false
