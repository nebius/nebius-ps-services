interface:
  display_name: "AI Stack"
  short_description: "Select AI components from a frozen subsystem contract"
  default_prompt: "Use $ai-stack to freeze the workload, consume $ai-agent-design's four-class behavior and policy contract or mark a direct stack-only classification provisional, prefer Pydantic AI as the portable Python model and agent runtime, justify specialist or provider-native escape hatches, classify every component and evidence claim, and define validation and switch conditions."
policy:
  allow_implicit_invocation: true
