interface:
  display_name: "AI Stack"
  short_description: "Select an effective, efficient AI stack that meets requirements"
  default_prompt: "Use $ai-stack to freeze the workload, select an effective, efficient AI stack that satisfies the requirements, classify every component and evidence claim, and define validation and switch conditions."
policy:
  allow_implicit_invocation: true
