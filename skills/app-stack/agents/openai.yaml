interface:
  display_name: "App Stack"
  short_description: "Select application stacks and logical handoffs"
  default_prompt: "Use $app-stack to select the smallest justified application stack, route undecided AI-specific layers through $ai-stack, and return logical-only decisions for any scaffold handoff."
policy:
  allow_implicit_invocation: true
