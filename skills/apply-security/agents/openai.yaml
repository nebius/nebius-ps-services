interface:
  display_name: "Apply Security"
  short_description: "Security review, advice, and safe patching"
  default_prompt: "Review this design, code, diff, or repository for security risks. Advise within the current scope and apply only authorized low-risk remediations."
policy:
  allow_implicit_invocation: true
