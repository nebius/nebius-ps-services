interface:
  display_name: "Brainstorm"
  short_description: "Evidence-first brainstorming with source-ranked context"
  default_prompt: "Use $brainstorm to explore an idea, gather the right repo/internal/vendor context, consult $design and $system-design-rules for major decisions when available, challenge assumptions, and discuss options without making code changes."
policy:
  allow_implicit_invocation: true
