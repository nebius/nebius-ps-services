interface:
  display_name: "Commit"
  short_description: "Create fast local whole-repo commits"
  default_prompt: "Use $commit to safely inspect and commit all current repository changes without pushing, including one exact local commit when a fresh explicit $worktree integrate delegates it."
policy:
  allow_implicit_invocation: false
