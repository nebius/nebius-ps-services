interface:
  display_name: "Design"
  short_description: "Design software solutions before implementation"
  default_prompt: "Use $design to understand requirements, inspect the existing system, route missing due diligence through $research, use $app-stack for undecided application layers, use $ai-stack for undecided AI layers, apply $system-design-rules, design vertical slices, compare alternatives, and create a /plan handoff."
policy:
  allow_implicit_invocation: true
