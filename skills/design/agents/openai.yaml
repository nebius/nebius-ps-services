interface:
  display_name: "Design"
  short_description: "Design software solutions before implementation"
  default_prompt: "Use $design to understand requirements, inspect the existing system, research missing technology knowledge, design the solution, compare alternatives, and create a /plan handoff."
policy:
  allow_implicit_invocation: true
