interface:
  display_name: "Design"
  short_description: "Design software and choose deterministic vs agentic AI flow"
  default_prompt: "Use $design to understand requirements, inspect the existing system, classify AI work as direct calls, deterministic workflows, or agents independently from graph or durability needs, route missing due diligence through $research, use $app-stack and $ai-stack for undecided layers, apply $system-design-rules, compare alternatives, and create a /plan handoff."
policy:
  allow_implicit_invocation: true
