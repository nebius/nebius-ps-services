interface:
  display_name: "Design"
  short_description: "Design software and delegate governed AI subsystem choices"
  default_prompt: "Use $design to understand requirements, inspect the existing system, route missing due diligence through $research, use $app-stack for undecided application layers, delegate AI behavior, topology, policy, and contracts once to $ai-agent-design, consume its scoped $ai-stack component decision, apply $system-design-rules, compare alternatives, and create a /plan handoff."
policy:
  allow_implicit_invocation: true
