interface:
  display_name: "Frontend Project"
  short_description: "Materialize exact React TypeScript Vite files"
  default_prompt: "Use $frontend-project to materialize exact React, TypeScript, and Vite files from fixed decisions and return a validated candidate manifest."
policy:
  allow_implicit_invocation: true
