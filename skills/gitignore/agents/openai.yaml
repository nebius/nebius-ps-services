interface:
  display_name: "Gitignore"
  short_description: "Create and standardize .gitignore files"
  default_prompt: "Use $gitignore to create or update a clean, stack-aware .gitignore for this repository."
policy:
  allow_implicit_invocation: true
