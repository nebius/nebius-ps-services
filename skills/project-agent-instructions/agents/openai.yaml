interface:
  display_name: "Project Agent Instructions"
  short_description: "Create project-specific agent instructions"
  default_prompt: "Use $project-agent-instructions after validated requirements and design to decide whether the selected project needs a root AGENTS.md."
policy:
  allow_implicit_invocation: false
