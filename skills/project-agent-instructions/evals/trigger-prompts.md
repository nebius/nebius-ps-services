# Trigger prompts

## Should trigger only through a coordinator

- Task Implementer has finished validating its managed requirements and design;
  route to `$project-agent-instructions` before the coordinator contract commit.
- Agentic SDLC has a current design and needs the
  project-agent-instructions decision
  before auto-steering and plan creation.

## Should not trigger

- Create a README for this project.
- Implement this feature without running Task Implementer or Agentic SDLC.
- Add a temporary rule for only this task.
- Update my global `~/.codex/AGENTS.md`.
- Create an `AGENTS.override.md` in every subdirectory.
