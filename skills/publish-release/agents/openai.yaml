interface:
  display_name: "Publish Release"
  short_description: "Generate GitHub Release publication artifacts"
  default_prompt: "Use $publish-release to create CHANGELOG.md, publish-release.sh, and a tag-driven GitHub Actions release workflow with artifact verification."
