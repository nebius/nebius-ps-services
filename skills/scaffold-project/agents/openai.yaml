interface:
  display_name: "Scaffold Project"
  short_description: "Own topology, approval, and guarded scaffold apply"
  default_prompt: "Use $scaffold-project to assign repository topology and path owners, bind exact candidate inputs, and apply only an explicitly approved digest."
policy:
  allow_implicit_invocation: false
