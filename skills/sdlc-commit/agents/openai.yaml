interface:
  display_name: "SDLC Commit"
  short_description: "Seal and promote one validated feature"
  default_prompt: "Use $sdlc-commit to seal the final integration tip for one completed Agentic SDLC feature, fast-forward the unchanged project feature branch to that exact SHA, record promotion evidence, and clean internal execution resources without pushing."
policy:
  allow_implicit_invocation: false
