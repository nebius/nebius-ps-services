interface:
  display_name: "SDLC Create Design"
  short_description: "Create SDLC design.md from requirements, context, and code evidence"
  default_prompt: "Use $sdlc-create-design to create initial evidence-backed design or perform classifier-admitted failure-driven redesign with positive system-contract proof, required approval, stable FEAT IDs, and a new fingerprint only when design actually changes."
policy:
  allow_implicit_invocation: false
