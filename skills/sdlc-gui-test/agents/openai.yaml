interface:
  display_name: "SDLC GUI Test"
  short_description: "Evaluate GUI flows with the required harness"
  default_prompt: "Use $sdlc-gui-test to execute the declared browser harness, including computer-use when required, refresh accessibility state after each action, and record correlated durable evidence."
policy:
  allow_implicit_invocation: false
