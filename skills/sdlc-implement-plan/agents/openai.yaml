interface:
  display_name: "SDLC Implement Plan"
  short_description: "Implement locked feature plan"
  default_prompt: "Use $sdlc-implement-plan to implement Agentic SDLC production code for one feature while staying inside the locked plan and its vertical end-to-end slice when present."
policy:
  allow_implicit_invocation: false
