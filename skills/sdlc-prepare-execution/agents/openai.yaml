interface:
  display_name: "Prepare SDLC Execution"
  short_description: "Prepare isolated SDLC feature execution"
  default_prompt: "Use $sdlc-prepare-execution to prepare or safely replan the active Agentic SDLC feature integration worktree on the exact promotion branch and actual remote-default identity, binding full task-definition digests and preserving every active or completed task while appending only resource-free corrective waves whose results prove the exact regression oracle."
policy:
  allow_implicit_invocation: false
