interface:
  display_name: "Prepare SDLC Execution"
  short_description: "Prepare isolated SDLC feature execution"
  default_prompt: "Use $sdlc-prepare-execution to prepare the active Agentic SDLC feature integration worktree before TDD."
policy:
  allow_implicit_invocation: false
