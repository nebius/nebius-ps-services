interface:
  display_name: "SDLC Start"
  short_description: "Run prompt-bound Agentic SDLC"
  default_prompt: "Use $sdlc-start workspace init [project-folder] to create a private prompt workspace, then use $sdlc-start run <prompt-path-or-unique-filename> to start, resume, or steer the prompt-bound Agentic SDLC workflow."
policy:
  allow_implicit_invocation: false
