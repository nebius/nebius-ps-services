interface:
  display_name: "SDLC Start"
  short_description: "Coordinate SDLC run state"
  default_prompt: "Use $sdlc-start to coordinate the Agentic SDLC loop by reading specs, steering input, and local state, refreshing auto-steering when needed, encouraging a safe live experiment environment when useful, selecting the next feature, and choosing exactly one next skill."
policy:
  allow_implicit_invocation: false
