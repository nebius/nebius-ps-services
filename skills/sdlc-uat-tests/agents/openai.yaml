interface:
  display_name: "SDLC UAT Tests"
  short_description: "Run product-level UAT"
  default_prompt: "Use $sdlc-uat-tests to validate the full Agentic SDLC product before PR creation, using the requirements Live Experiment Environment only when it is safe and allowed."
policy:
  allow_implicit_invocation: false
