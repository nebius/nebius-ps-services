interface:
  display_name: "SDLC Update Documents"
  short_description: "Update docs after SDLC implementation evidence"
  default_prompt: "Use $sdlc-update-documents to update project-facing documentation from implemented and evaluated Agentic SDLC evidence, including end-to-end slice evidence when multi-layer behavior is documented, plus resolved steering."
policy:
  allow_implicit_invocation: false
