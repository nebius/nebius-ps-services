interface:
  display_name: "SDLC Validate Codes"
  short_description: "Validate build, lint, type, config, and review quality"
  default_prompt: "Use $sdlc-validate-codes to run project-native build, parse, lint, type, import, dependency, configuration, and locked-slice boundary validation for the current Agentic SDLC feature, then use $code-review in review-only mode before marking the feature validated."
policy:
  allow_implicit_invocation: false
