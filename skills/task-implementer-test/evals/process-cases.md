# Supplemental Process Cases

These cases preserve detailed workflow and output-quality expectations.
`trigger-prompts.csv` is the sole canonical trigger authority; this document
does not define skill routing. Contract tests remain required for lifecycle and
output assertions; canonical CSV validation does not replace them.

## Post-Routing Mode Assertions

| Canonical routing row | Required process and output result |
| --- | --- |
| `task-test-positive-01` | Run the deterministic profile only, report every skipped live capability and the exact private report path, and leave no real application. |
| `task-test-positive-02` | Semantically verify the owned multi-tier application and Task Implementer workflow, complete the report, and remove the exact active lifecycle and owned runtime resources. |
| `task-test-positive-03` | Produce the same evidence while retaining exactly one current owned generation and reporting its inspection and destroy commands. |
| `task-test-positive-04` | Leave the retained owned generation absent or return `ALREADY_DESTROYED`, while preserving reports and lifecycle history. |
