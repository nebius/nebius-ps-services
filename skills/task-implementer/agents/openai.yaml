interface:
  display_name: "Task Implementer"
  short_description: "Sequential brownfield task implementation"
  default_prompt: "Use $task-implementer to break a brownfield request into ordered task-1..task-n steps, implement them sequentially with fresh Codex sessions, and maintain a markdown handoff checkpoint."
policy:
  allow_implicit_invocation: true
